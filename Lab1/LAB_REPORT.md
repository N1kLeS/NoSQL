# Отчёт

## Version 0.2

### Что сделал:

- Пользователь хранится в bucket `users`: key — `userId`, value — JSON с полями `id`, `name`, `email`.
- `userId` выбран ключом, потому что пользователь запрашивается по идентификатору без поиска по всем записям.
- Приложение сохраняет и читает данные через HTTP API Riak KV. Этот способ поддерживается используемой версией Riak и не требует устаревшего Java-клиента.
- Для отсутствующего пользователя возвращается `HTTP 404`, для некорректных данных — `HTTP 400` с сообщением на русском языке.

### Что нужно сделать:

- Расширить пользователя настройками уведомлений.
- Реализовать просмотр истории действий пользователя оператором.
- Реализовать временную пользовательскую сессию с проверкой срока действия.
- Реализовать кэширование профиля пользователя.
- Реализовать атомарный счётчик посещений страницы.
- Перейти к кластеру Riak и исследовать перезапуск одного узла.
- Проверить сохранение и восстановление данных.
- Сделать вывод о применимости Riak KV.
